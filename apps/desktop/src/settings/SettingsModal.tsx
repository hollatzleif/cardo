/*
 * The cramped settings modal is gone: settings now live on the fullscreen
 * SettingsPage (sidebar navigation + grouped rows, see SettingsPage.tsx).
 * This file stays as a thin re-export so historical references keep working.
 */
export { SettingsPage } from './SettingsPage';
